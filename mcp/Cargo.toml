[package]
name = "ontoskills-mcp"
version = "0.8.0"
edition = "2024"
description = "Local MCP server for OntoSkills ontologies"
license = "MIT"

[dependencies]
oxigraph = "0.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
