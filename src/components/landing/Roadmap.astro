---
const phases = [
  {
    status: 'done',
    phase: 'Phase 1',
    title: 'OntoCore',
    description: 'Source skills compile into validated ontologies and reusable knowledge nodes',
  },
  {
    status: 'done',
    phase: 'Phase 2',
    title: 'OntoMCP',
    description: 'Package-aware MCP runtime serves enabled ontologies through a small tool surface',
  },
  {
    status: 'done',
    phase: 'Phase 3',
    title: 'OntoSkills',
    description: 'Managed CLI orchestrates installs, updates, enable/disable, and local home layout',
  },
  {
    status: 'done',
    phase: 'Phase 4',
    title: 'OntoStore',
    description: 'Official store publishes compiled skills and supports remote installs by qualified id',
  },
  {
    status: 'planned',
    phase: 'Phase 5',
    title: 'Community Imports',
    description: 'Third-party registries, source repository imports, and stricter release/update automation',
  },
];

const statusStyles = {
  done: 'border-accent-mint text-accent-mint',
  wip: 'border-accent-cyan text-accent-cyan animate-pulse',
  planned: 'border-text-muted text-text-muted',
  future: 'border-border text-text-muted opacity-60',
};

const statusIcons = {
  done: '✓',
  wip: '🔨',
  planned: '💡',
  future: '🔮',
};
---

<section id="roadmap" class="py-20">
  <div class="section-shell max-w-5xl">
    <div class="text-center mb-12">
      <div class="section-kicker mx-auto mb-4">Build status</div>
      <h2 class="text-3xl sm:text-4xl font-bold text-text-primary mb-4">
        <span class="gradient-text">Roadmap</span>
      </h2>
      <p class="text-text-muted">
        This evolves as we build.
      </p>
    </div>

    <div class="glass-panel relative rounded-[2rem] px-4 py-8">
      <!-- Timeline line -->
      <div class="absolute left-8 md:left-1/2 top-0 bottom-0 w-px bg-border md:-translate-x-px"></div>

      <!-- Phases -->
      <div class="space-y-8">
        {phases.map((phase, i) => (
          <div class={`relative flex items-start gap-6 ${i % 2 === 1 ? 'md:flex-row-reverse' : ''}`}>
            <!-- Marker -->
            <div class={`absolute left-8 md:left-1/2 w-8 h-8 rounded-full border-2 bg-bg-primary flex items-center justify-center text-sm -translate-x-1/2 ${statusStyles[phase.status]}`}>
              {statusIcons[phase.status]}
            </div>

            <!-- Content -->
            <div class={`ml-20 md:ml-0 md:w-1/2 px-2 ${i % 2 === 1 ? 'md:pr-20 md:text-right' : 'md:pl-20'}`}>
              <span class="text-xs uppercase tracking-wider text-text-muted">{phase.phase}</span>
              <h3 class="text-xl font-bold text-text-primary mt-1">{phase.title}</h3>
              <p class="text-text-muted mt-2">{phase.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <div class="mt-12 text-center">
      <p class="text-text-muted text-sm">
        Track progress on <a href="https://github.com/mareasw/ontoskills" class="text-accent-cyan hover:underline">GitHub</a> and the official store at <a href="https://github.com/mareasw/ontostore" class="text-accent-cyan hover:underline">ontostore</a>
      </p>
    </div>
  </div>
</section>
